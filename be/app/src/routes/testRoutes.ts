// import { verifySession } from "supertokens-node/recipe/session/framework/express/index.js";
// import app from "../app.ts";
// import { checkContext, protectedContext } from "../service/testService.ts";

// // Check context route
// app.get("/context", (req, res) => checkContext(req, res, app));

// app.get("/protected", verifySession(), (req, res) =>
//   protectedContext(req, res, app)
// );
